//Function examples 
function sorted(array) {
	var sortedArray = array.sort();
  var reversedArray = sortedArray.reverse();
  //Have the function return the array
  return reversedArray;
}

//Define an array of numbers
a = [1,2,4,32,2,16];
//Call the function, passing the array
console.log("Our sorted array is ", sorted(a))



function reverseString(str){
    //
    var array = str.split('');
    //reverse sort it
    var reversed = array.sort().reverse();
    //convert back to a str
    var new Str = reversed.join
    }










































